import { DivisionStylingDirective } from './division-styling.directive';

describe('DivisionStylingDirective', () => {
  it('should create an instance', () => {
    const directive = new DivisionStylingDirective();
    expect(directive).toBeTruthy();
  });
});
